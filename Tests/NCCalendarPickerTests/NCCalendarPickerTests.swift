import XCTest
@testable import NCCalendarPicker

final class NCCalendarPickerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NCCalendarPicker().text, "Hello, World!")
    }
}
